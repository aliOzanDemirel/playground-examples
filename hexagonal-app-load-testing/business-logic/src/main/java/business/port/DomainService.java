package business.port;

// domain service is much like a transactional script that executes logic related to many entities
// not really needed (no example) in this context as this module is not pure domain logic, so simple
// example logic here is just be encapsulated in use cases as they are the orchestrator business logic
public @interface DomainService {
}
